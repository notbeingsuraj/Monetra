# Download Inter font files from Google Fonts and place them here:

# Inter-Regular.ttf (weight 400)

# Inter-Medium.ttf (weight 500)

# Inter-SemiBold.ttf (weight 600)

# Inter-Bold.ttf (weight 700)

#

# Download from: https://fonts.google.com/specimen/Inter

# Or use the google_fonts package (already configured to auto-download).

# The pubspec.yaml also declares local font assets — download and place them here,

# OR remove the `fonts:` block from pubspec.yaml to use google_fonts network CDN.
